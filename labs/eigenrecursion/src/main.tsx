import React from 'react';
import ReactDOM from 'react-dom/client';
import EigenrecursionNotebook from '../eigenrecursion_convergence';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <EigenrecursionNotebook />
  </React.StrictMode>
);
